export enum EBookRequestStatus {
    ACCEPTED = 'ACCEPTED',
    DENIED = 'DENIED',
    PENDING = 'PENDING',
}

export enum EBookRequestType {
    BORROWING = 'BORROWING',
    RETURNING = 'RETURNING',
}